// The Full Book Import has moved to /import.
// This page redirects to keep old links working.

import { redirect } from "next/navigation";

export default function FullImportRedirect() {
  redirect("/import");
}
